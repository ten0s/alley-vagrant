#
# alley-vagrant project
#

mkdir -p ~/projects/vagrant/alley-dev
cd ~/projects/vagrant/alley-dev
vagrant init ten0s/centos-6.5
vagrant up
vagrant ssh-config
git clone git@github.com:PowerMeMobile/alley-setup.git

Vagrantfile relevent parts:
````
$script = <<SCRIPT
/vagrant/alley-setup/setup/setup-env centos6
/vagrant/alley-setup/setup/setup-autostart
SCRIPT

config.vm.provision "shell", inline: $script
````

vagrant up

sudo cp /home/bms/.erlang.cookie .
rabbitmqctl add_user alley alley
rabbitmqctl set_permissions -p / alley ".*" ".*" ".*"
rabbitmqctl delete_user guest
